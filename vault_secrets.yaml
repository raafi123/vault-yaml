all:
  hosts:
    localhost:      
      secret_tuple_template:
        - "location"
        - "key"
        - "value"

      vault_address: http://127.0.0.1:8200

      root_token: root

      vault_skip_verify: true

      container_name: hcvault1

      username: admin

      